<Window x:Class="PureXS.Views.FacilityTokenDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Facility Setup"
        Width="450" Height="200"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        WindowStyle="SingleBorderWindow"
        Background="{DynamicResource WindowBg}"
        FontFamily="Segoe UI">

    <Grid Margin="24,20,24,16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="Enter your PureChart facility token:"
                   Foreground="{DynamicResource TextPrimary}"
                   FontSize="14"
                   Margin="0,0,0,12"/>

        <TextBox Grid.Row="1"
                 x:Name="TokenInput"
                 FontSize="13"
                 FontFamily="Consolas"
                 Padding="8,6"
                 Background="{DynamicResource FieldBg}"
                 Foreground="{DynamicResource TextPrimary}"
                 BorderBrush="{DynamicResource BorderSubtle}"
                 BorderThickness="1"
                 CaretBrush="{DynamicResource TextPrimary}"/>

        <StackPanel Grid.Row="3"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right">
            <Button Content="Cancel"
                    Width="80" Height="30"
                    Margin="0,0,10,0"
                    IsCancel="True"
                    Click="Cancel_Click"
                    Background="{DynamicResource ToolbarBtnBg}"
                    Foreground="{DynamicResource TextSecondary}"
                    BorderBrush="{DynamicResource BorderSubtle}"
                    FontSize="13"
                    Cursor="Hand"/>
            <Button Content="OK"
                    Width="80" Height="30"
                    IsDefault="True"
                    Click="OK_Click"
                    Background="{DynamicResource DentalBlue}"
                    Foreground="White"
                    BorderBrush="{DynamicResource DentalBlue}"
                    FontSize="13"
                    FontWeight="SemiBold"
                    Cursor="Hand"/>
        </StackPanel>
    </Grid>
</Window>
